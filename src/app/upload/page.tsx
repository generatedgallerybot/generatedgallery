import UploadClient from './UploadClient';

export const metadata = {
  title: 'Upload Model Assets | Generated Gallery',
  description: 'Share LoRAs, checkpoints, workflows, and datasets with the Generated Gallery community.',
};

export default function UploadPage() {
  return <UploadClient />;
}
